version: '3'

tasks:
  setup:
    desc: Install git hooks (run once after cloning)
    cmds:
      - lefthook install

  test:
    desc: Run all flight tests
    cmds:
      - task -d forgejo test
      - task -d onetimesecret test
